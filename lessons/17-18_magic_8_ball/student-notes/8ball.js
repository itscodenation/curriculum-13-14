function getFortune() {
  // Fill in your code here!
}

// Use this function once you have your text
// to place it on the page. For instance,
// fillInAnswer("THIS IS JUST RANDOM TEXT")
// will write that on the page!
function fillInAnswer(str) {
  $('#answer').text(str);
}
